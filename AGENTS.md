# AGENTS.md

## Git 커밋

* 커밋은 가능한 한 작고 하나의 목적에 집중되도록 작성한다.
* 하나의 커밋에는 하나의 논리적인 변경 사항만 포함한다.
* 서로 관련 없는 변경 사항을 하나의 커밋으로 묶지 않는다.
* 현재 작업과 관련 없는 코드 정리, 리팩터링, 포맷 변경 등을 커밋에 포함하지 않는다.
* 커밋 메시지는 간결하고 사람이 읽기 쉽게 작성한다.
* 커밋 메시지는 구현 방법이나 내부적인 작업 과정보다는 **무엇이 변경되었는지**를 명확하게 표현한다.
* `update`, `fix stuff`, `changes`, `work` 등 변경 내용을 알 수 없는 모호한 커밋 메시지를 사용하지 않는다.
* 커밋하기 전에 staged된 변경 사항을 확인하고, 모두 하나의 논리적인 변경에 해당하는지 확인한다.

## 스타일링

* CSS 파일을 사용하지 않는다.
* Chakra UI 컴포넌트의 프롭스를 통해서 스타일을 지정하되, 색깔은 지정하지 않는다.