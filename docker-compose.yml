services:
  fitzza:
    image: fitzza-frontend:dev
    build:
      context: .
      dockerfile: Dockerfile
    container_name: fitzza
    ports:
      - "80:80"
    restart: unless-stopped
    environment:
      - NODE_ENV=production